import React from 'react';
import { StatusBar } from 'react-native';
import AppNavigator from './frontend/navigation/AppNavigtor';

const App = () => {
  
  return <AppNavigator />;   // now handles Splash + SignIn screens
};

export default App;
